# ゲームデザイン実装 TODO

この文書は、`Documents/ADRs/GameDesign/` に記録された方針を、実装順に落とした追跡用 TODO です。

運用ルール:

* 未着手は `【 】`
* 完了は `【x】`
* 仕様が確定していない項目は、実装前に関連 ADR を更新または新規 ADR を追加してから進める

## 進め方の原則

* 先に「1遠征の最小プロトタイプ」を作り、その後に複数遠征からなる1RUNへ拡張する
* カード追加より先に、遠征・街・OverHunt・キーストーンの受け皿を作る
* 魔法だけ先行させず、騎士・ガンナーも比較できる最低限の土台を揃える
* 承認済みADRと現実装がズレている箇所は、新規拡張より先に揃える

## Phase 1: 戦闘ルールの未確定点を閉じる

### 1-1. シールド仕様の確定

* 【x】 `ADR-002` のシールド方針を再確認し、現行実装との差分を明文化する
* 【x】 シールドを `永続 + 上限付き` にするか、`ターン制 + 加算型` を正式採用するか決める
* 【x】 決定内容に合わせて `ADR-002` のステータスを更新する
* 【x】 シールド再付与時の扱いを定義する
* 【x】 シールドのUI文言を定義する

### 1-2. 戦闘報酬ルールのADR準拠化

* 【x】 一般戦闘報酬が `ADR-005` の出現方針と一致しているか確認する
* 【x】 HP満タン時に回復報酬を出さない制御を入れる
* 【x】 レベルアップ候補が存在しない時にレベルアップ報酬を除外する
* 【x】 MOD候補の最低母数を整理し、報酬の単調化を防ぐ
* 【x】 報酬抽選ロジックを定数ではなく調整しやすい形へ寄せる

### 1-3. 複数枚プレイ前提の危険要素チェック

* 【x】 `0マナ / 0CT` 系カードやMODの有無を棚卸しする
* 【 】 マナ回復、コスト軽減、CT短縮の危険な組み合わせをレビュー観点として整理する
* 【 】 無限ループ対策を「バランスで抑える」のか「システム制限を追加する」のか判断基準を決める

## Phase 2: 1遠征 + 1街 の最小プロトタイプを作る

このPhaseは最終的な「複数遠征からなる1RUN」ではない。キーストーン選択と次遠征への遷移は後続Phaseで実装する。

### 2-1. RunStateと進行状態の拡張

* 【x】 `RunState` に遠征進行を表す状態を追加する
* 【x】 `RunState` に所持ゴールドを追加する
* 【x】 `RunState` にOverHunt用の保留ゴールドを追加する
* 【x】 `RunState` に街遷移と帰還判定に必要な状態を追加する

### 2-2. 最小遠征フロー

* 【x】 通常戦闘を複数回こなしてボス戦へ進む最小フローを作る
* 【x】 遠征クリア後に `街へ帰る / OverHuntへ進む` の分岐を作る
* 【x】 一般戦闘終了時に、ADR-005の三択報酬を即時解決するよう修正する
* 【x】 一般戦闘終了時に、ゴールドを所持ゴールドへ即時加算するよう修正する
* 【x】 OverHunt進行中に死亡した場合の失敗処理を作る

### 2-3. 最小OverHunt

* 【x】 OverHunt用の追加戦闘を最低1戦作る
* 【x】 OverHunt戦では `保留ゴールドのみ獲得` を実装する
* 【x】 OverHunt戦では回復報酬を出さないようにする
* 【x】 OverHunt戦後に `帰還 / 続行` を選べるようにする
* 【x】 帰還時に保留ゴールドを所持ゴールドへ変換する

### 2-4. 最小街機能

* 【x】 街を安全地帯として遷移できるようにする
* 【x】 街でHP回復を購入できるようにする
* 【x】 街でレベルアップを購入できるようにする
* 【x】 街でMOD付与を購入できるようにする
* 【x】 街で新カード獲得を購入候補に含める

### 2-5. 複数遠征RUNへの拡張

* 【x】 街から次の遠征を開始する遷移を実装する
* 【x】 RUN全体の遠征数、最終クリア条件、失敗条件を定義する
* 【x】 遠征をまたいで保持する状態と、遠征開始時にリセットする状態を整理する

## Phase 3: キーストーンと重み制御を入れる

### 3-1. タグ基盤

* 【x】 カードに必要なタグを定義する
* 【x】 MODに必要なタグを定義する
* 【x】 キーストーン、エンジン、汎用アイテムに必要なタグを定義する
* 【x】 報酬候補の抽選でタグ重みを扱える基盤を作る

### 3-2. キーストーン選択

* 【x】 RUN開始時にキーストーンを1つ選ぶフローを作る
* 【x】 `魔法の水晶` を追加する
* 【x】 `騎士の盾` を追加する
* 【x】 `マスケット銃` を追加する
* 【x】 キーストーン取得時の初期カード付与を実装する（対応カード未実装の軸は後続で設定）
* 【x】 キーストーン取得後のカード/MOD/エンジン重み上昇を実装する

### 3-3. エンジンと汎用アイテムの入口

* 【 】 エンジンの出現枠をボス報酬へ組み込む
* 【 】 汎用アイテムの出現枠を街や特別報酬へ組み込む
* 【 】 完全ロックではなく重み調整で混成ビルド余地を残す

## Phase 4: 三すくみが比較できる最低限の初期コンテンツを揃える

### 4-1. 魔法使い軸

* 【x】 現在の `集中 -> 魔法` ループがキーストーン選択から自然に始まるようにつなぐ
* 【 】 `炎術師の杖` を実装する
* 【 】 `氷術師のクローク` を実装する
* 【 】 魔法軸の立ち上がりが遅い代わりに上振れが強いかを確認する

### 4-2. 騎士軸

* 【 】 `シールドバッシュ` を実装または現行カードから対応づける
* 【 】 `2段切り` を実装または現行カードから対応づける
* 【 】 `グレートソード` を実装する
* 【 】 `アマデウスの鎧` を実装する
* 【 】 騎士が安定軸として成立するかを確認する

### 4-3. ガンナー軸

* 【 】 `砲撃` を実装する
* 【 】 `弾丸カード` の生成と消費ルールを実装する
* 【 】 `追加弾倉` を実装する
* 【 】 `豪炎火薬` を実装する
* 【 】 ガンナーが高機動・高手数・高リスクとして成立するかを確認する

## Phase 5: バランス調整と検証

### 5-1. 進行テンポ

* 【 】 一般遠征だけでも攻略可能なバランスになっているか確認する
* 【 】 OverHuntが必須解になっていないか確認する
* 【 】 街の商品価格とゴールド供給量の釣り合いを確認する
* 【 】 強化版ボスを入れる場合の到達期待値を確認する

### 5-2. 報酬テンポ

* 【 】 MOD報酬がレベルアップ報酬に見劣りしていないか確認する
* 【 】 HP回復報酬が強化選択を潰しすぎていないか確認する
* 【 】 新カード獲得タイミングが特別報酬として機能しているか確認する

### 5-3. アーキタイプ差

* 【 】 魔法使い / 騎士 / ガンナーの防御手段の差が明確か確認する
* 【 】 魔法使い / 騎士 / ガンナーの立ち上がり速度の差が明確か確認する
* 【 】 魔法使い / 騎士 / ガンナーの操作難度の差が明確か確認する
* 【 】 どれか1軸が最適解化していないか確認する

## 保留事項

* 【 】 ミッションを採用するか、採用する場合の目的・発令条件・報酬・精算タイミングをADRで決める
* 【 】 OverHuntの強化版ボスの出現段階、戦闘内容、保留ゴールド報酬を設計・実装する
* 【 】 ボス報酬、街、イベントの責務分担をさらに細かく分ける必要があるか判断する
